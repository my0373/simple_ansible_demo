---
- name: generate user with random hashed password, store passwords in local files named 'password-(first-name) playbook'
  hosts: all
  vars_files:
    - users.yml
  tasks:
    - name: User task
      user:
        name: "{{ item.name }}"
        password: password
        comment: "{{ item.first + ' ' + item.middle + ' ' + item.last }}"
        state: present
        uid: "{{ item.uid }}"
      loop: "{{ users }}"
